# BMAD framework install — regenerate locally with `npx bmad-method install`
# (do NOT ignore _bmad-output/ — that's committed project content: PRD, architecture, stories)
_bmad/

# AI tool local session state / config
.claude/
.opencode/

# TODO: confirm whether .agents/ holds shared project config or local state.
# If it's machine/session-specific, uncomment the line below:
.agents/

# Environment variables / secrets
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# Build output
dist/
build/

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/